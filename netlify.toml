[build]
  base    = "VaultGuard-Portal-main"
  command = "pnpm install --frozen-lockfile && pnpm --filter @workspace/vaultguard build"
  publish = "artifacts/vaultguard/dist/public"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
